rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ Ultra-ouvert pour démarrer (lecture/écriture publiques)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
